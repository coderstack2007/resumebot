# Resume Telegram Bot

Телеграм-бот для сбора резюме пользователей. Поддерживает русский и узбекский языки. Собирает ФИО, возраст, номер телефона, фото и информацию о желаемой вакансии, после чего сохраняет данные в базу данных.

---

## Требования

- PHP 8.0 или выше
- Composer
- Токен Telegram Bot (получен через [@BotFather](https://t.me/BotFather))

---

## Установка

**1. Клонируйте репозиторий**

```bash
git clone https://github.com/coderstack2007/resumebot.git
cd ResumeBot1
```

**2. Установите зависимости через Composer**

```bash
composer install
```

Проект использует библиотеку [irazasyed/telegram-bot-sdk](https://github.com/irazasyed/telegram-bot-sdk) версии `^3.15`.

**3. Укажите токен бота**

Откроем файл `src/BotSettings.php` и вставьте ваш токен:

```php
<?php
namespace App;

class BotSettings
{
    public const TOKEN = 'ВАШ_ТОКЕН_БОТА';
}
```

> ⚠️ Никогда не коммитьте токен в репозиторий. Добавьте `src/BotSettings.php` в `.gitignore` или используйте переменные окружения.

---

## Запуск

Бот работает в режиме **polling** — он сам регулярно опрашивает серверы Telegram за новыми сообщениями. Никакого веб-сервера и домена не требуется.

```bash
php polling.php
```

После запуска в терминале появится сообщение:

```
🤖 Бот запущен в режиме polling...
```

Бот работает, пока процесс не остановлен (Ctrl+C). Для продакшена рекомендуется запускать через `supervisor` или аналогичный процесс-менеджер, чтобы бот автоматически перезапускался при сбоях.

---

## Структура проекта

```
ResumeBot1/
├── src/
│   ├── BotSettings.php              # Токен бота
│   ├── Database.php                 # Подключение и работа с БД
│   ├── Info/
│   │   ├── RuInfoHandler.php        # Обработка шагов (русский)
│   │   └── UzInfoHandler.php        # Обработка шагов (узбекский)
│   ├── Keyboards/
│   │   ├── ru/
│   │   │   ├── LanguageKeyboard.php
│   │   │   ├── NameKeyboard.php
│   │   │   ├── NumberKeyboard.php   # Кнопка "Поделиться номером"
│   │   │   ├── CitiesKeyboard.php
│   │   │   └── JobsKeyboard.php
│   │   └── uz/
│   │       ├── LanguageKeyboard.php
│   │       ├── NameKeyboard.php
│   │       ├── NumberKeyboard.php
│   │       ├── CitiesKeyboard.php
│   │       └── JobsKeyboard.php
│   ├── Checking/
│   │   ├── ru/Check.php             # Валидация ввода (русский)
│   │   └── uz/Check.php             # Валидация ввода (узбекский)
│   ├── Cities/
│   │   ├── ru/Cities.php            # Список регионов и городов (русский)
│   │   └── uz/Cities.php            # Список регионов и городов (узбекский)
│   ├── Jobs/
│   │   ├── Ru/Jobs.php              # Список вакансий (русский)
│   │   └── Uz/Jobs.php              # Список вакансий (узбекский)
│   ├── Backs/
│   │   ├── ru/BackHandler.php       # Обработка кнопки "Назад" (русский)
│   │   └── uz/BackHandler.php       # Обработка кнопки "Назад" (узбекский)
│   └── images/                      # Сохранённые фото пользователей
├── polling.php                      # Точка входа — запуск бота
├── composer.json
├── composer.lock
└── README.md
```

---

## Как работает бот

Процесс сбора резюме проходит пошагово:

| Шаг | Что собирается | Способ ввода |
|-----|----------------|--------------|
| 1 | ФИО | Текст |
| 2 | Возраст | Число |
| 3 | Номер телефона | Текст или кнопка «Поделиться номером» |
| 4 | Фото | Отправка изображения |
| 5 | Регион | Выбор из кнопок |
| 6 | Город | Выбор из кнопок |
| 7 | Вакансия | Выбор из кнопок |
| 8 | Подтверждение | Да / Назад |

На каждом шаге доступна кнопка «Назад» для возврата к предыдущему шагу. После подтверждения данные сохраняются в базу данных.

На шаге 3 пользователь может либо ввести номер телефона вручную, либо нажать кнопку **«📱 Поделиться номером»**, которая использует нативную функцию Telegram для передачи контакта.

---

## Автозагрузка (Autoload)

Проект использует PSR-4 автозагрузку. Все классы лежат в папке `src/` и доступны под пространством имён `App\`:

```json
{
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
```

После любых изменений в структуре папок или добавления новых классов выполняйте:

```bash
composer dump-autoload
```

---

## Зависимости

| Библиотека | Версия | Назначение |
|------------|--------|------------|
| irazasyed/telegram-bot-sdk | ^3.15 | Взаимодействие с Telegram Bot API |

---

## .gitignore (рекомендация)

```
vendor/
src/BotSettings.php
src/images/
```